# Features

Feature-based directory structure for smart components.
Each feature should contain:

- Components (smart components)
- Hooks (feature-specific hooks)
- API (Tauri command wrappers for this feature)
